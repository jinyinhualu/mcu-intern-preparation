# 2026-09-11 学习记录：函数指针、回调和电脑端命令分发

## 今日目标

- 用电脑端 C 程序模拟冷链终端命令行。
- 区分命令表中的函数指针和分发模块向上层发通知的回调。
- 练习可写命令缓冲区、`argc/argv` 参数转发、命令查找和错误路径。

## 本次搭建

- 新建 `c-language/13-integrated-Command_Dispatch_Lab/` 综合练习工程。
- 建立 `CommandHandler`、`CommandLogCallback`、`CommandEntry` 和 `CommandContext` 接口。
- 在 `main.c` 中准备 `led`、`add`、`status` 三个电脑端命令处理函数。
- 加入正常命令、坏参数、未知命令、空命令、回调注册和回调清除测试。
- 按固定工作模式保留 `command_dispatch.c` 的核心 TODO，没有直接完成全部实现。

## 当前状态

- 题目框架和固定测试已建立。
- 需要先自行分析至少 20 分钟，再逐步完成拆参、查表、分发和回调保存。
- 已用 `gcc -Wall -Wextra -std=c11 main.c command_dispatch.c -o command_dispatch_lab.exe` 编译，无编译器警告。
- 基线运行结果为 `2/20 passed`，程序退出码为 `1`；目前只有空行拆分和找不到命令两条默认行为测试通过。
- 失败集中在：非空命令拆分、已有命令查找、处理函数指针调用、分发计数、错误计数和日志回调。
- 当前第一组分析记录应包括：可能原因 A 是 `command_split_line()` 仍返回 `0`；可能原因 B 是 `command_dispatch()` 尚未调用解析函数。验证方法是先单独观察 `argc/argv`，再观察 `ctx->dispatch_count` 是否变化。

## 故障复盘模板

每组失败测试记录：

```text
可能原因 A：
可能原因 B：
我的验证方法：
```

## 下一步

1. 编译运行基线，确认 TODO 导致的失败范围。
2. 先完成 `command_split_line()`，只验证解析相关测试。
3. 再完成 `command_find()` 与处理函数指针调用。
4. 最后完成回调保存、错误计数和无回调路径。
