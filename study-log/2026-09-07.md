# 2026-09-07 学习记录：栈、堆、全局区、静态区与内存错误

## 今日目标

- 搞清楚栈、堆、全局区、静态区的区别。
- 故意制造一次越界和一次野指针问题，观察编译器警告或调试结果。
- 练习用地址输出和编译选项验证自己的判断。

## 实际完成

- 搭了一道综合练习题的骨架，题目覆盖栈、堆、全局区、静态区、越界和野指针。
- 把练习要求整理成可直接手敲的题面，没有提前给出完整答案。
- 提醒了适合观察问题的编译选项：`-Wall -Wextra -Warray-bounds -O2`、`-Wuninitialized`、`-fsanitize=address,undefined`。

## 能独立完成

- 区分局部变量、静态局部变量、全局变量和堆内存。
- 用 `malloc/free` 管理堆内存。
- 知道数组越界和野指针都属于未定义行为。

## 只能看懂

- 为什么有些越界只有在优化打开后才更容易被编译器发现。
- 为什么不同编译器对同一段未定义行为的反应不一样。

## 完全不会

- 只靠肉眼稳定预测一次未定义行为的实际运行结果。
- 复杂程序里追踪内存错误的完整调试流程。

## 今天暴露的问题

- 当前环境没有现成的 `gcc` / `clang` / `cl` 可直接运行，暂时只能先把练习题写好。
- 对数组越界和野指针的“警告 vs. 运行时崩溃”边界还不够熟。

## 下一步

- 找到可用编译器后，把这份代码实际编译一遍，记录具体警告和崩溃位置。
- 再补一个指针专项练习，把“栈地址、堆地址、释放后地址”的差别单独练透。
